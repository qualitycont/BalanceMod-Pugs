// also taken from rgl

exec "bug_base"

servercfgfile "bug_arena" // sets server cfg to this config so that it gets autoexeced on level change

mp_timelimit "0"            // unsets server timelimit
mp_winlimit "3"             // sets number of wins to win each half to 2
mp_windifference "0"        // unsets windifference
mp_maxrounds "0"            // unsets maxrounds

mp_tournament "0"       // disables tournament mode since its completely incompatible with arena

say "KOTH ( BEST OF 5 ) CONFIG EXECUTED"